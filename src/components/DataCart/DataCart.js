import React from 'react';
import './DataCart.css'

const DataCart = ({cartData}) => {
    console.log(cartData.name);
    
    
    return (
        <div>
            {/* <h1>Name: {props.cartData.name}</h1> */}
        </div>
    );
};

export default DataCart;